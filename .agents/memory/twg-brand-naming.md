---
name: TWG brand naming
description: Correct brand name is "TWG", but the artifact slug/dir is "tgw-network" — do not "fix" the slug to match.
---

# TWG brand naming

The correct brand name is **TWG Network** (not "TGW"). All user-facing copy uses "TWG".

**Gotcha:** the artifact directory and slug are `tgw-network` (with the old, incorrect "tgw" ordering), and the workflow/component title is "TGW Network". These were NOT renamed — only visible site copy was corrected.

**Why:** the user asked only to fix site copy; renaming the slug/dir would change routing/paths and was not requested.

**How to apply:** when adding new copy, always write "TWG". Do not assume the brand is "TGW" just because the folder is `tgw-network`. Do not rename the slug/dir unless the user explicitly asks.
