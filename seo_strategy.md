# SEO Strategy

## In scope
- Public marketing pages in `artifacts/tgw-network`
- Core public routes: `/`, `/practitioners`, `/clients`, `/network`, `/pricing`, `/manifesto`, `/about`, `/apply`, `/register`
- Crawlability, indexation, on-page metadata, social sharing, AI crawler visibility, structured data, and obvious internal-linking issues

## Out of scope
- Authenticated dashboards and admin pages (none identified in the current codebase)
- API-only packages in the workspace unless they directly serve public marketing HTML or crawl files

## Target audience
- Organisations seeking experienced fractional or interim practitioners
- Independent senior practitioners evaluating whether to join the network

## Primary keywords
- wasted generation
- the wasted generation
- TWG Network
- fractional consultants network
- interim consultant network
- fractional experts network

## Dismissed categories
- None yet

## Notes
- User concern for this scan: the site does not appear prominently for searches related to "wasted generation".
- Current public site is a Vite + React SPA using Wouter, so static HTML limitations are central to this audit.
