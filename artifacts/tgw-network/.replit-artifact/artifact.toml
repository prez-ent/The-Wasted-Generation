kind = "web"
previewPath = "/"
title = "TGW Network"
version = "1.0.0"
id = "artifacts/tgw-network"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18573

[services.development]
run = "pnpm --filter @workspace/tgw-network run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tgw-network", "run", "build" ]
publicDir = "artifacts/tgw-network/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18573"
BASE_PATH = "/"
